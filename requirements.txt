# Core dependencies for the coffee shop workflow simulation project
#
# numpy: numerical routines and Poisson sampling used by the simulator
# matplotlib: plotting library for generating result figures

numpy>=1.20
matplotlib>=3.0

# Optional, but useful for data analysis in notebooks or future extensions
pandas>=2.0

# Install instructions:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
