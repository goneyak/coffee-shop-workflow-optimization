experiment_name: preorder_buffer_large
sim_time_hours: 8
dt: 0.005
runs: 300

arrival:
  type: constant
  lambda_per_hour: 60

service:
  till:
    mu_per_hour: 65
  shots:
    mu_per_hour: 85
  milk:
    mu_per_hour: 75

policy:
  preorder_buffer:
    enabled: true
    # buffer_limit is a legacy field; the current implementation ignores it